import React from "react";
import LDButton from "./components/LDButton";

export default function RevealDice({

                    }) {
  return (
    <LDButton title={"Reveal Dice"} titleStyle={{fontWeight: "bold", paddingLeft: 20, paddingRight: 20, paddingTop: 5, paddingBottom: 5}}/>

  )
}